# Rust
/target
**/*.rs.bk
*.pdb

# Node
node_modules
**/dist
*.tsbuildinfo

# IDE
/.vscode
/.idea
*.iml
*.swp
.DS_Store

# Claude Code local-machine settings (permissions, etc.)
.claude/settings.local.json

# Local environment
.env
.env.local

# Cargo
# Keep Cargo.lock for the workspace (we ship a binary).
# The reference clone of agent-browser is checked in by the user but not
# tracked as part of agent-ctrl itself.
/agent-browser

# Build artifacts
/dist
/bin/native
# Per-platform CLI binaries copied here by `npm run build:native` for npm packaging.
# Built fresh per release; not tracked.
/bin/agent-ctrl-*

# Next.js build output for the landing site
**/.next

# Per-machine skill installs (npx skills add ...)
/.agents

# OS
Thumbs.db
