{
  "name": "agent-ctrl-monorepo",
  "private": true,
  "description": "Top-level workspace for the agent-ctrl TypeScript packages.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/k4cper-g/agent-ctrl.git"
  },
  "homepage": "https://github.com/k4cper-g/agent-ctrl#readme",
  "bugs": {
    "url": "https://github.com/k4cper-g/agent-ctrl/issues"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
